const createFramework = (creator, releaseDate, language);

const angular = createFramework("Google", "2016", "TypeScript");
const react = createFramework("React", "2013", "JavaScript");

console.log(angular, react);
